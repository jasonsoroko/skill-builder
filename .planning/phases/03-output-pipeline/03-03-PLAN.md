---
phase: 03-output-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/skill_builder/agents/packager.py
  - src/skill_builder/agents/__init__.py
  - src/skill_builder/progress.py
  - src/skill_builder/conductor.py
  - src/skill_builder/cli.py
  - pyproject.toml
  - tests/test_packager_agent.py
  - tests/test_progress.py
  - tests/test_conductor.py
  - tests/test_cli.py
autonomous: true
requirements: [PKG-01, PKG-02, PKG-03, CORE-10]

must_haves:
  truths:
    - "Packager assembles output folder with SKILL.md, SETUP.md, references/, scripts/, assets/, LICENSE.txt (MIT) at the correct deploy target path"
    - "Deploy targets write to correct paths: repo -> .claude/skills/{name}/, user -> ~/.claude/skills/{name}/, package -> .skill-builder/output/{name}/"
    - "Pipeline prints specific installation verification instructions after packaging"
    - "Rich CLI shows current phase, active agent, iteration count, and elapsed time during pipeline run"
    - "Non-TTY environments get plain text log lines instead of Rich Live display"
    - "Final summary panel shows total time, total cost, evaluator scores, feedback loop counts, output path, verification instructions"
    - "Verbose mode shows budget summary at phase boundaries"
  artifacts:
    - path: "src/skill_builder/agents/packager.py"
      provides: "PackagerAgent conforming to BaseAgent Protocol"
      exports: ["PackagerAgent"]
    - path: "src/skill_builder/progress.py"
      provides: "PipelineProgress display with TTY detection and fallback"
      exports: ["PipelineProgress"]
    - path: "src/skill_builder/cli.py"
      provides: "Updated CLI with Rich progress and final summary panel"
      contains: "PipelineProgress"
  key_links:
    - from: "src/skill_builder/agents/packager.py"
      to: "filesystem"
      via: "Path.mkdir + write_text for output folder assembly"
      pattern: "mkdir.*parents=True"
    - from: "src/skill_builder/conductor.py"
      to: "src/skill_builder/progress.py"
      via: "progress.phase_start/phase_complete callbacks replacing print()"
      pattern: "self.progress"
    - from: "src/skill_builder/cli.py"
      to: "src/skill_builder/progress.py"
      via: "PipelineProgress injected into Conductor"
      pattern: "PipelineProgress"
    - from: "src/skill_builder/cli.py"
      to: "src/skill_builder/progress.py"
      via: "summary_panel called after pipeline completes"
      pattern: "summary_panel"
---

<objective>
Build the PackagerAgent that assembles and deploys the final .skill output, and add Rich CLI progress display throughout the entire pipeline with a final summary panel.

Purpose: The PackagerAgent is the final pipeline step -- it takes validated drafts and produces the deployable output. Rich CLI progress replaces raw print() calls in the conductor with a live status display that shows phase, agent, iteration, and timing. The final summary panel provides a build receipt. This completes the full end-to-end pipeline.
Output: PackagerAgent, PipelineProgress, updated conductor and CLI -- the pipeline is fully functional.
</objective>

<execution_context>
@/Users/jasonsoroko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasonsoroko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-output-pipeline/03-CONTEXT.md
@.planning/phases/03-output-pipeline/03-RESEARCH.md
@.planning/phases/03-output-pipeline/03-01-SUMMARY.md
@.planning/phases/03-output-pipeline/03-02-SUMMARY.md

<interfaces>
<!-- Types and contracts from Plans 01 and 02 -->

From src/skill_builder/agents/base.py:
```python
@runtime_checkable
class BaseAgent(Protocol):
    def run(self, **kwargs: Any) -> BaseModel | dict[str, Any]: ...
```

From src/skill_builder/models/production.py (extended in Plan 01):
```python
class SkillDraft(BaseModel):
    content: str
    line_count: int
    has_frontmatter: bool
    reference_files: dict[str, str] | None = None

class SetupDraft(BaseModel):
    content: str
    has_prerequisites: bool
    has_quick_start: bool
```

From src/skill_builder/models/evaluation.py:
```python
class EvaluationDimension(BaseModel):
    name: str
    score: int = Field(ge=1, le=10)
    feedback: str
    passed: bool

class EvaluationResult(BaseModel):
    dimensions: list[EvaluationDimension]
    overall_pass: bool
    iteration: int
```

From src/skill_builder/models/brief.py:
```python
class SkillBrief(BaseModel):
    name: str
    deploy_target: Literal["repo", "user", "package"]
    brief_name: str  # computed
    ...
```

From src/skill_builder/budget.py:
```python
@dataclass
class TokenBudget:
    budget_usd: float = 25.0
    total_cost_usd: float = 0.0
    @property
    def exceeded(self) -> bool: ...
    @property
    def remaining_usd(self) -> float: ...
```

From src/skill_builder/conductor.py (modified in Plan 02):
```python
class Conductor:
    def __init__(self, brief, store, budget, agents=None): ...
    def run(self, state=None) -> PipelineState: ...
    # _run_phase currently uses print() for phase banners
```

From src/skill_builder/cli.py:
```python
@click.command()
def build(brief, dry_run, resume, verbose, budget, force): ...
    # Currently uses click.echo for output
    # conductor = Conductor(brief=skill_brief, store=store, budget=token_budget)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PackagerAgent + Rich PipelineProgress</name>
  <files>
    src/skill_builder/agents/packager.py
    src/skill_builder/progress.py
    pyproject.toml
    tests/test_packager_agent.py
    tests/test_progress.py
  </files>
  <behavior>
    PackagerAgent:
    - Test: PackagerAgent conforms to BaseAgent Protocol
    - Test: run() accepts skill_draft dict, setup_draft dict, brief SkillBrief; returns dict with "package_path" key
    - Test: Assembles folder with SKILL.md, SETUP.md at root
    - Test: Creates references/, scripts/, assets/ subdirectories
    - Test: Writes LICENSE.txt with MIT license text including current year
    - Test: When skill_draft has reference_files, writes each to references/ directory
    - Test: deploy_target="repo" writes to .claude/skills/{brief_name}/
    - Test: deploy_target="user" writes to ~/.claude/skills/{brief_name}/
    - Test: deploy_target="package" writes to .skill-builder/output/{brief_name}/
    - Test: Returns dict with package_path string and verification_instructions string
    - Test: verification_instructions includes specific trigger question example

    PipelineProgress:
    - Test: PipelineProgress.__init__ accepts verbose bool
    - Test: phase_start(phase, agent) records state
    - Test: phase_complete(phase, elapsed) records state
    - Test: eval_score(name, score, passed) formats as "9/10 PASS" or "4/10 FAIL"
    - Test: budget_display(spent, budget_total) formats as "$5.82 / $25.00 (23.3%)"
    - Test: summary_panel accepts total_time, total_cost, eval_dimensions, gap_loops, validation_loops, output_path, verification_instructions
    - Test: When console.is_terminal is False, phase_start/phase_complete use plain text output
  </behavior>
  <action>
    1. **Add rich dependency** to pyproject.toml:
       - Run `uv add "rich>=14.0,<15"` to add rich to dependencies

    2. **Create PackagerAgent** in `src/skill_builder/agents/packager.py`:
       - Conforms to BaseAgent Protocol: run(**kwargs) -> dict[str, Any]
       - No LLM calls needed -- pure Python file operations
       - run() expects: skill_draft (dict), setup_draft (dict), brief (SkillBrief)
       - Steps:
         a. Resolve output path via `_resolve_deploy_path(brief.deploy_target, brief.brief_name)` helper
         b. Create output directory with `Path.mkdir(parents=True, exist_ok=True)`. Catch PermissionError and provide helpful message.
         c. Write SKILL.md from skill_draft["content"]
         d. Write SETUP.md from setup_draft["content"]
         e. Create subdirectories: references/, scripts/, assets/
         f. If skill_draft has reference_files (not None), write each file to references/
         g. Write LICENSE.txt with MIT license template (current year, skill brief name)
         h. Build verification_instructions string: "To verify the skill works:\n1. Open Claude Code in a project\n2. Ask: '{trigger question based on brief.name}'\n3. Check that the skill activates and provides accurate guidance\n\nOutput files:\n{list each file with line count}"
         i. Return {"package_path": str(output_path), "verification_instructions": verification_instructions}
       - Helper: `_resolve_deploy_path(deploy_target: str, tool_name: str) -> Path`
         * "repo" -> Path(".claude/skills") / tool_name
         * "user" -> Path.home() / ".claude" / "skills" / tool_name
         * "package" -> Path(".skill-builder/output") / tool_name
         * ValueError for unknown targets

    3. **Create PipelineProgress** in `src/skill_builder/progress.py`:
       - Uses Rich Console, Panel, Table, Text, Live
       - `__init__(verbose: bool = False)`: Create Console(), detect is_terminal, init state vars (_phase, _agent, _iteration, _elapsed, _start_time)
       - `phase_start(phase: str, agent: str)`: If TTY, update live display. If not TTY, console.print plain text line like `[{phase}] Starting {agent}...`
       - `phase_complete(phase: str, elapsed: float)`: If TTY, update live display. If not TTY, console.print `[{phase}] Complete ({elapsed:.1f}s)`
       - `eval_score(name: str, score: int, passed: bool)`: Display `{name}: 9/10 PASS` (green) or `4/10 FAIL` (red). Per CONTEXT.md: "Evaluator scores should display as 9/10 PASS or 4/10 FAIL -- clean, scannable"
       - `budget_display(spent: float, budget_total: float)`: Display `${spent:.2f} / ${budget_total:.2f} ({pct:.1f}%)`. Only shown when verbose=True. Per CONTEXT.md: "$5.82 / $25.00 (23.3%)"
       - `summary_panel(total_time: float, total_cost: float, eval_dimensions: list[dict], gap_loops: int, validation_loops: int, output_path: str, verification_instructions: str)`: Render a Rich Panel with Table showing all metrics. Per CONTEXT.md: "The final summary panel should feel like a build receipt: time, cost, scores, output path, next step"
       - Non-TTY: All methods degrade to simple console.print text lines (Rich handles this automatically via Console.is_terminal)

    4. **Write tests:**
       - test_packager_agent.py: Use tmp_path fixture for all file operations. Verify folder structure, file contents, deploy target paths (mock Path.home() for user target). Verify reference_files are written. Verify LICENSE.txt content.
       - test_progress.py: Test formatting methods. Mock Console to control is_terminal. Verify eval_score output format. Verify budget_display format. Verify summary_panel includes all expected fields.
  </action>
  <verify>
    <automated>uv run pytest tests/test_packager_agent.py tests/test_progress.py -x --timeout=10</automated>
  </verify>
  <done>
    - PackagerAgent assembles correct folder structure at correct deploy target path
    - PackagerAgent writes reference files, LICENSE.txt, verification instructions
    - PipelineProgress shows phase/agent/iteration/elapsed with TTY fallback
    - Evaluator scores display as "9/10 PASS" / "4/10 FAIL"
    - Budget display formats as "$5.82 / $25.00 (23.3%)"
    - Summary panel includes all required metrics
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire PackagerAgent + Rich progress into conductor and CLI</name>
  <files>
    src/skill_builder/agents/__init__.py
    src/skill_builder/conductor.py
    src/skill_builder/cli.py
    tests/test_conductor.py
    tests/test_cli.py
  </files>
  <action>
    1. **Update agents/__init__.py:**
       - Import and export PackagerAgent
       - Remove StubPackagerAgent from _default_agents (it was the last stub for Phase 3)

    2. **Update _default_agents() in conductor.py:**
       - Replace `StubPackagerAgent()` with `PackagerAgent()`
       - Update imports

    3. **Inject PipelineProgress into Conductor:**
       Per RESEARCH.md open question 4: "Inject a PipelineProgress object into the Conductor (optional parameter). The conductor calls progress.phase_start() and progress.phase_complete() instead of print(). When no progress object is provided (e.g., tests), fall back to current print() behavior."
       - Add `progress: PipelineProgress | None = None` parameter to `Conductor.__init__`
       - In `_run_phase`, replace `print(f"  [{phase_label}] Starting...")` with:
         ```python
         if self.progress:
             self.progress.phase_start(phase_label, agent_key)
         else:
             print(f"  [{phase_label}] Starting...")
         ```
       - Replace completion print similarly with `self.progress.phase_complete(phase_label, elapsed)`
       - After validation phase, if progress exists, call `self.progress.eval_score(dim["name"], dim["score"], dim["passed"])` for each dimension in the result
       - In budget exceeded section, if progress and verbose, call `self.progress.budget_display(self.budget.total_cost_usd, self.budget.budget_usd)`
       - At phase boundaries (after _store_result, before budget check), if progress and verbose: call `self.progress.budget_display(self.budget.total_cost_usd, self.budget.budget_usd)`

    4. **Update _build_kwargs for PACKAGING in conductor.py:**
       - Currently passes skill_draft, setup_draft, brief
       - The PackagerAgent is pure Python and needs these exact kwargs -- no change needed to kwargs, just verify they match

    5. **Update _store_result for PACKAGING in conductor.py:**
       - Currently does `pass` for PACKAGING. Update to store the package result:
         ```python
         elif phase == PipelinePhase.PACKAGING:
             state.package_path = dumped.get("package_path")
         ```
       - Note: PipelineState does not have a package_path field. Add it: go to models/state.py and add `package_path: str | None = None` field. This is a minor model extension needed for the packager result.

    6. **Update CLI (src/skill_builder/cli.py):**
       - Import PipelineProgress
       - Create PipelineProgress(verbose=verbose) before conductor
       - Pass progress to Conductor: `Conductor(brief=skill_brief, store=store, budget=token_budget, progress=progress)`
       - After conductor.run() completes:
         * If result.phase == COMPLETE: call progress.summary_panel() with total time, total cost, evaluation dimensions, gap_loop_count, validation_loop_count, output path (from result), verification instructions
         * Extract eval_dimensions from result.evaluation_results[-1]["dimensions"] if available
         * Extract verification_instructions from the packager output or generate default ones
       - Remove the existing click.echo-based completion summary (replaced by Rich summary panel)
       - The dry-run output can stay as click.echo (Rich is not needed for dry-run)

    7. **Update tests:**
       - test_conductor.py: Verify Conductor accepts progress=None without breaking. Verify when progress is provided, phase_start/phase_complete are called. Use mock PipelineProgress.
       - test_cli.py: Update to account for Rich progress integration. The autouse _default_agents fixture should still work. May need to mock PipelineProgress or verify Rich output.
  </action>
  <verify>
    <automated>uv run pytest tests/test_conductor.py tests/test_cli.py tests/test_packager_agent.py tests/test_progress.py -x --timeout=10</automated>
  </verify>
  <done>
    - _default_agents uses real PackagerAgent (only StubIntakeAgent remains as stub)
    - Conductor accepts optional PipelineProgress and uses it for phase banners
    - Conductor falls back to print() when no progress provided (test compatibility)
    - CLI creates PipelineProgress and passes to Conductor
    - CLI displays Rich summary panel on completion with time, cost, scores, path, verification
    - Verbose mode shows budget at phase boundaries
    - All tests pass, full suite green
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x --timeout=10` -- full suite green
- `uv run pytest tests/test_packager_agent.py tests/test_progress.py tests/test_conductor.py tests/test_cli.py -v` -- all tests pass
- PackagerAgent writes correct files to correct deploy target paths
- Rich progress displays cleanly in TTY and degrades in non-TTY
- Summary panel shows all required metrics
</verification>

<success_criteria>
- PackagerAgent assembles output folder and resolves deploy target paths correctly
- PipelineProgress provides Rich Live status during pipeline execution
- Non-TTY environments get plain text fallback
- Final summary panel is a complete build receipt
- Conductor fully wired with all real agents (only StubIntakeAgent remains)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-pipeline/03-03-SUMMARY.md`
</output>

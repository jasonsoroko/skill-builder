---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/skill_builder/__init__.py
  - src/skill_builder/models/__init__.py
  - src/skill_builder/models/brief.py
  - src/skill_builder/models/state.py
  - src/skill_builder/models/harvest.py
  - src/skill_builder/models/synthesis.py
  - src/skill_builder/models/production.py
  - src/skill_builder/models/evaluation.py
  - examples/exa-tavily-firecrawl.json
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_models.py
  - .gitignore
  - .python-version
autonomous: true
requirements:
  - CORE-01
must_haves:
  truths:
    - "SkillBrief model validates a well-formed JSON brief and rejects malformed ones with specific error messages"
    - "All pipeline data models (state, harvest, synthesis, production, evaluation) exist with correct fields and can round-trip through JSON serialization"
    - "Example brief file loads and validates successfully as a SkillBrief"
    - "Project installs via uv with all dependencies and pytest runs"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, dependencies, CLI entry point, pytest config, ruff config"
      contains: "skill-builder"
    - path: "src/skill_builder/models/brief.py"
      provides: "SkillBrief Pydantic model with typed URLs"
      exports: ["SkillBrief", "SeedUrl"]
    - path: "src/skill_builder/models/state.py"
      provides: "PipelineState and PipelinePhase enum"
      exports: ["PipelineState", "PipelinePhase"]
    - path: "examples/exa-tavily-firecrawl.json"
      provides: "First target skill brief fixture"
    - path: "tests/test_models.py"
      provides: "Model validation tests"
  key_links:
    - from: "examples/exa-tavily-firecrawl.json"
      to: "src/skill_builder/models/brief.py"
      via: "SkillBrief.model_validate_json()"
      pattern: "model_validate_json"
    - from: "src/skill_builder/models/state.py"
      to: "src/skill_builder/models/brief.py"
      via: "PipelineState references brief_name derived from SkillBrief"
      pattern: "brief_name"
---

<objective>
Set up the project scaffold and define all Pydantic data models that form the type contracts for the entire pipeline.

Purpose: Everything in skill-builder depends on these models -- the conductor, checkpoint store, budget tracker, agents, and CLI all import from this layer. Building models first prevents the scavenger hunt anti-pattern and establishes the serialization contracts for checkpoint persistence.

Output: Installable Python package with all data models, example brief, test infrastructure, and passing model validation tests.
</objective>

<execution_context>
@/Users/jasonsoroko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasonsoroko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold and Pydantic data models</name>
  <files>
    pyproject.toml,
    .gitignore,
    .python-version,
    src/skill_builder/__init__.py,
    src/skill_builder/models/__init__.py,
    src/skill_builder/models/brief.py,
    src/skill_builder/models/state.py,
    src/skill_builder/models/harvest.py,
    src/skill_builder/models/synthesis.py,
    src/skill_builder/models/production.py,
    src/skill_builder/models/evaluation.py,
    tests/__init__.py,
    tests/conftest.py,
    tests/test_models.py
  </files>
  <behavior>
    - Test: valid SkillBrief JSON with all required fields (name, description, seed_urls with typed URLs, tool_category, scope, required_capabilities, deploy_target) loads successfully
    - Test: SkillBrief with missing required field raises ValidationError with specific field name in error
    - Test: SkillBrief with invalid URL type (not docs|github|api_schema|blog) raises ValidationError
    - Test: SkillBrief optional fields (target_api_version, max_pages) get sensible defaults when omitted
    - Test: PipelineState round-trips through model_dump_json() / model_validate_json() preserving all fields including datetime and enum
    - Test: PipelinePhase enum values are strings (str mixin) and JSON-serializable
    - Test: All model modules (harvest, synthesis, production, evaluation) export their primary models
  </behavior>
  <action>
    1. Create pyproject.toml with:
       - Project metadata: name="skill-builder", version="0.1.0", requires-python=">=3.12,<3.15"
       - Dependencies: click>=8.3,<9; pydantic>=2.12,<3; anthropic>=0.84,<1; langsmith>=0.7,<1; tenacity>=9.1,<10
       - Dev dependencies: pytest>=8.0; pytest-timeout; ruff>=0.15; mypy>=1.13
       - Entry point: [project.scripts] skill-builder = "skill_builder.cli:main"
       - Pytest config: [tool.pytest.ini_options] testpaths=["tests"], timeout=10
       - Ruff config: [tool.ruff] line-length=100, target-version="py312"
       - Build system: hatchling>=1.25

    2. Create .python-version with "3.12"

    3. Create .gitignore with standard Python ignores plus .skill-builder/state/ (checkpoint dir)

    4. Create src/skill_builder/__init__.py with __version__ = "0.1.0"

    5. Create src/skill_builder/models/brief.py:
       - SeedUrl model: url (HttpUrl or str), type (Literal["docs", "github", "api_schema", "blog"])
       - SkillBrief model: name (str, min 1 char), description (str), seed_urls (list[SeedUrl], min 1), tool_category (str), scope (str), required_capabilities (list[str], min 1), deploy_target (Literal["repo", "user", "package"]), target_api_version (str | None = None), max_pages (int = 50)
       - Use Pydantic v2 patterns: str | None = None for optional, Field(min_length=1) for required strings
       - Class method or validator to derive brief_name from name field (slugified)

    6. Create src/skill_builder/models/state.py:
       - PipelinePhase(str, Enum) with all phases from research: INITIALIZED, INTAKE, HARVESTING, ORGANIZING, GAP_ANALYZING, RE_HARVESTING, LEARNING, MAPPING, DOCUMENTING, VALIDATING, RE_PRODUCING, PACKAGING, COMPLETE, FAILED
       - PipelineState(BaseModel) with: phase, brief_name, all phase output fields as dict | None = None (placeholders for Phase 2+), gap_loop_count, validation_loop_count, total_input_tokens, total_output_tokens, total_cost_usd, error str | None, started_at datetime, updated_at datetime
       - Use Field(default_factory=...) for datetime fields

    7. Create src/skill_builder/models/harvest.py:
       - HarvestPage: url, title, content, source_type, content_hash (str | None = None)
       - HarvestResult: pages (list[HarvestPage]), total_pages (int), fetch_plan (dict | None = None)

    8. Create src/skill_builder/models/synthesis.py:
       - ResearchCategory: name, content (list[str])
       - CategorizedResearch: categories (list[ResearchCategory]), source_count (int)
       - GapReport: is_sufficient (bool), identified_gaps (list[str]), recommended_search_queries (list[str])
       - KnowledgeModel: canonical_use_cases (list[str]), required_parameters (list[str]), common_gotchas (list[str]), best_practices (list[str]), anti_patterns (list[str]), dependencies (list[str]), minimum_viable_example (str), trigger_phrases (list[str])

    9. Create src/skill_builder/models/production.py:
       - SkillDraft: content (str), line_count (int), has_frontmatter (bool)
       - SetupDraft: content (str), has_prerequisites (bool), has_quick_start (bool)

    10. Create src/skill_builder/models/evaluation.py:
        - EvaluationDimension: name (str), score (int, ge=1, le=10), feedback (str), passed (bool)
        - EvaluationResult: dimensions (list[EvaluationDimension]), overall_pass (bool), iteration (int)

    11. Create src/skill_builder/models/__init__.py re-exporting all primary models.

    12. Create tests/conftest.py with fixtures:
        - sample_brief_dict: returns a valid brief dict
        - sample_brief_json: returns valid brief as JSON string
        - tmp_state_dir: uses tmp_path to create a temporary state directory

    13. Create tests/test_models.py with tests per the behavior block above.

    14. Run: cd /Users/jasonsoroko/Documents/Code/skill-builder && uv venv && uv pip install -e ".[dev]"

    15. Run: pytest tests/test_models.py -x
  </action>
  <verify>
    <automated>cd /Users/jasonsoroko/Documents/Code/skill-builder && uv run pytest tests/test_models.py -x --timeout=10</automated>
  </verify>
  <done>
    - pyproject.toml exists with all dependencies, entry point, and tool configs
    - All 7 model files exist under src/skill_builder/models/ with exported types
    - tests/test_models.py passes: valid brief loads, invalid brief rejected with specific errors, PipelineState JSON round-trip works
    - Package installs successfully via uv pip install -e ".[dev]"
  </done>
</task>

<task type="auto">
  <name>Task 2: Example skill brief and model completeness</name>
  <files>
    examples/exa-tavily-firecrawl.json
  </files>
  <action>
    Create examples/exa-tavily-firecrawl.json as the first target skill brief per user decision. This file doubles as documentation and smoke test.

    Contents should represent a real brief for building a skill about using Exa + Tavily + Firecrawl together for deep research crawling:
    - name: "exa-tavily-firecrawl"
    - description: A skill for deep research crawling using Exa semantic search, Tavily web search, and Firecrawl site crawling together
    - seed_urls: 3-5 typed URLs covering docs sites for each tool, at least one GitHub repo, at least one API schema URL
    - tool_category: "research"
    - scope: "Using Exa, Tavily, and Firecrawl together for comprehensive research crawling"
    - required_capabilities: ["semantic search with Exa", "web search with Tavily", "site crawling with Firecrawl", "parallel execution", "result deduplication"]
    - deploy_target: "user"
    - target_api_version: null (let the pipeline discover versions)

    After creating the file, validate it loads as a SkillBrief:
    ```
    python -c "from skill_builder.models.brief import SkillBrief; import json; b = SkillBrief.model_validate_json(open('examples/exa-tavily-firecrawl.json').read()); print(f'Valid: {b.name}')"
    ```
  </action>
  <verify>
    <automated>cd /Users/jasonsoroko/Documents/Code/skill-builder && uv run python -c "from skill_builder.models.brief import SkillBrief; b = SkillBrief.model_validate_json(open('examples/exa-tavily-firecrawl.json').read()); print(f'Brief: {b.name}, URLs: {len(b.seed_urls)}, Caps: {len(b.required_capabilities)}')"</automated>
  </verify>
  <done>
    - examples/exa-tavily-firecrawl.json exists with realistic content for the first target skill
    - File validates successfully as a SkillBrief with no errors
    - Brief has typed seed URLs, required capabilities, and deploy target
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_models.py -v` -- all model tests pass
- `uv run python -c "from skill_builder.models import SkillBrief, PipelineState, PipelinePhase"` -- imports succeed
- `uv run python -c "from skill_builder.models.brief import SkillBrief; SkillBrief.model_validate_json(open('examples/exa-tavily-firecrawl.json').read())"` -- example brief validates
- `uv run ruff check src/ tests/` -- no lint errors
</verification>

<success_criteria>
- Package installs and all imports work
- All Pydantic models defined with correct field types and defaults
- Model JSON round-trip serialization works (critical for checkpoint persistence in Plan 02)
- Example brief validates against SkillBrief schema
- Test suite passes with at least 7 tests covering valid/invalid brief, state round-trip, enum serialization
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
